---

## 【全局配置】Claude Code 自动批准规则

### 自动批准的操作（不需要确认）

1. ✅ 所有文件写入和修改操作
2. ✅ 所有代码生成和编辑操作
3. ✅ 运行 npm run dev、npm run build、npm run test 等开发命令
4. ✅ 运行 git status、git add、git commit、git push 等Git命令
5. ✅ 安装和更新npm依赖包

### 需要确认的操作（必须手动确认）

1. ❌ 删除文件或目录
2. ❌ 运行 sudo 命令
3. ❌ 运行可能修改系统配置的命令
4. ❌ 向外部发送网络请求的命令

### AI 行为准则

- 所有上述自动批准的操作，执行前不需要向用户确认
- 执行完成后只需要告知用户结果即可
- 如果遇到不确定的操作，先询问用户再执行
